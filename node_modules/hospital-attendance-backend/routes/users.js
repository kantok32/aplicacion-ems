// Rutas para gestión de usuarios (si es necesario, ej: por el Director)
const express = require('express');
const router = express.Router();
// const userController = require('../controllers/userController');
// const authMiddleware = require('../middlewares/authMiddleware');

// router.get('/', authMiddleware.protect, authMiddleware.restrictTo('Director'), userController.getAllUsers);
// ... otras rutas de gestión de usuarios

module.exports = router; 