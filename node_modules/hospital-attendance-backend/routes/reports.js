// Rutas para la generación de reportes (Director)
const express = require('express');
const router = express.Router();
// const reportController = require('../controllers/reportController');
// const authMiddleware = require('../middlewares/authMiddleware');

// router.get('/csv', authMiddleware.protect, authMiddleware.restrictTo('Director'), reportController.generateCsvReport);
// router.get('/pdf', authMiddleware.protect, authMiddleware.restrictTo('Director'), reportController.generatePdfReport);

module.exports = router; 