// Funciones de utilidad

// Ejemplo: función para manejar errores async
// exports.catchAsync = fn => {
//   return (req, res, next) => {
//     fn(req, res, next).catch(next);
//   };
// };

// Otras utilidades (manejo de fechas, etc.) 