// Rutas para el registro de asistencia
const express = require('express');
const router = express.Router();
// const attendanceController = require('../controllers/attendanceController');
// const authMiddleware = require('../middlewares/authMiddleware');

// router.post('/check-in', authMiddleware.protect, attendanceController.checkIn);
// router.post('/check-out', authMiddleware.protect, attendanceController.checkOut);
// router.get('/', authMiddleware.protect, attendanceController.getAttendanceHistory);
// router.get('/today', authMiddleware.protect, attendanceController.getTodaysAttendance);
// router.get('/all', authMiddleware.protect, authMiddleware.restrictTo('Director'), attendanceController.getAllAttendance);

module.exports = router; 